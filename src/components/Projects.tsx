import SectionHeader from "./SectionHeader";

const Projects = () => {
  return (
    <section className="h-screen container mx-auto">
      <SectionHeader title="Projects That I've Done" />
    </section>
  );
};

export default Projects;

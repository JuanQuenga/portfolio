import SectionHeader from "./SectionHeader";

const Contact = () => {
  return (
    <section className="h-screen container mx-auto">
      <SectionHeader title="Get In Contact With Me" />
    </section>
  );
};

export default Contact;
